import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'introduccion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
